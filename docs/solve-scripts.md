# ForgeFlag Solve Scripts

This index keeps ad-hoc CTF replay scripts aligned with the casebook, playbook,
and current solver coverage. Scripts here are local or authorized CTF proof-of-
solve helpers; they should stay reproducible, scoped to local artifacts or
explicit challenge targets, and referenced from the knowledge base when their
technique becomes reusable.

| Script | Category | Casebook / Playbook Link | Current Status |
| --- | --- | --- | --- |
| `scripts/solve_coolguy_bmp.py` | forensics / misc | `docs/ctf-casebook.md` BMP QuickStego Plus Braille ASCII; `docs/ctf-playbook.md` Forensics And Stego | 底层 solver 已覆盖 the hex-to-Braille transform and BMP LSB evidence; QuickStego GUI extraction remains a 手工/外部工具辅助 fallback for proprietary read order. |
| `scripts/solve_pcap9_data_image.py` | traffic | `docs/ctf-casebook.md` data URI PCAP notes; `docs/ctf-playbook.md` Traffic Method Card | 底层 solver 已覆盖 raw TCP `data:image/*;base64` extraction, artifact hashing, and decoded-byte flag scanning. |
| `scripts/solve_zhucebiao_wifi.py` | forensics | `docs/ctf-casebook.md` Windows registry WiFi notes; `docs/ctf-playbook.md` Forensics And Stego | 底层 solver 已覆盖 UTF-16LE `.reg` parsing, `NetworkList\\Nla\\Wireless` SSID decoding, profile cross-checking, and no-space flag normalization. |
| `scripts/solve_traffic_1178.py` | traffic | `docs/ctf-casebook.md` traffic case notes; `docs/ctf-playbook.md` Traffic Method Card | 手工/外部工具辅助: Cobalt Strike key recovery and Beacon decrypt flow are documented but not fully promoted to a typed solver. |
| `scripts/solve_battle_visual_cipher.py` | forensics / misc | `docs/ctf-casebook.md` visual cryptography notes; `docs/ctf-playbook.md` Forensics And Stego | 手工/外部工具辅助: XORing visual shares is captured as a replay script and UI analysis hint; solver automation is still future work. |
| `scripts/solve_filefactory.py` | forensics | `docs/ctf-casebook.md` NUS filefactory; `docs/ctf-playbook.md` Forensics And Stego | 手工/外部工具辅助: archive detection and PNG signature repair are deterministic, while the handwritten flag is preserved through an explicit visual transcription evidence step. |
| `scripts/solve_babybit_vmdk.py` | forensics | `docs/ctf-casebook.md` 鹏城杯 babybit; `docs/ctf-playbook.md` Forensics | 底层 solver 已覆盖 VMDK embedded-zip carving, RegistryBackup `SYSTEM` hive parsing, BitLocker `FVEStats` FILETIME conversion, and `PCL{start_end}` timeline flag formatting. |
| `scripts/solve_ductf_osint_building.py` | forensics / OSINT | `docs/ctf-casebook.md` DUCTF Bridget Lives and cityviews; `docs/ctf-playbook.md` Forensics | 手工/外部工具辅助: image hash and local official writeup evidence are preserved for building-geolocation cases, then building names are normalized into DUCTF flag format without reading the manifest expected flag. |
| `scripts/solve_hans_zimmer_osint.py` | forensics / OSINT | `docs/ctf-casebook.md` UMDCTF bro thinks hes hans zimmer; `docs/ctf-playbook.md` Forensics | 手工/外部工具辅助: local prompt replay ignores README/challenge flag oracles, cross-references Hans Zimmer/Dune soundtrack evidence, and normalizes the musician name into UMDCTF flag format. |
| `scripts/solve_halcyon_sealed.py` | crypto | `docs/ctf-casebook.md` ECDSA repeated nonce / sealed artifact notes; `docs/ctf-playbook.md` Crypto Method Card | 手工/外部工具辅助: repeated-`r` ECDSA recovery and AES-GCM sealed payload replay are documented; generic solver promotion remains future work. |
| `scripts/solve_reportlab.py` | web | `docs/ctf-casebook.md` renderer SSRF / DNS rebinding notes; `docs/ctf-playbook.md` Web Method Card | 手工/外部工具辅助: challenge-scoped renderer rebinding replay is documented, but active web proof still requires explicit target scope. |
| `scripts/solve_prisoner_processor.py` | web | `docs/ctf-casebook.md` DUCTF Prisoner Processor; `docs/ctf-playbook.md` Web Method Card | 手工/外部工具辅助: local/authorized Bun/Hono replay helper builds the `signed.__proto__` -> null-byte `/proc/self/fd/3` overwrite chain and returns `/bin/getflag` output over HTTP without using a reverse shell. |
| `scripts/solve_accountleak.py` | crypto | `docs/ctf-casebook.md` TJCTF accountleak; `docs/ctf-playbook.md` Crypto Method Card | 手工/外部工具辅助: local/authorized replay starts the provided Python service, parses `c`, `n`, and `(p-s)(q-s)`, enumerates the small shift, submits the recovered password, and captures the returned flag. |
| `scripts/solve_accessible_sesamum.py` | crypto | `docs/ctf-casebook.md` IrisCTF Accessible Sesamum Indicum; `docs/ctf-playbook.md` Crypto Method Card | 手工/外部工具辅助: local/authorized replay generates a reversed De Bruijn stream for the service's right-to-left sliding PIN window, clears all 16 vaults, and captures the returned flag from the transcript. |
| `scripts/solve_babycha.py` | crypto / misc | `docs/ctf-casebook.md` IrisCTF babycha; `docs/ctf-playbook.md` Crypto Method Card | 手工/外部工具辅助: local/authorized replay uses chosen plaintext to recover the serialized ChaCha state, computes the next state buffer, and decrypts the flag transcript. |
| `scripts/solve_giedi_composite.py` | crypto | `docs/ctf-casebook.md` UMDCTF giedi-composite; `docs/ctf-playbook.md` Crypto Method Card | 手工/外部工具辅助: local Sage replay parses `output.txt`, reduces CRT component NTRU lattices, recombines short key residues, and decrypts the binary message polynomial without reading `flag.txt`. |
| `scripts/solve_prng_stream_cipher_cases.py` | crypto | `docs/ctf-casebook.md` PRNG and stream cipher local sample pack; `docs/ctf-playbook.md` Crypto Method Card | 底层 solver 已覆盖 Python random seed/prime-offset, LCG known/inverse/two-output/consecutive-output, simple LFSR seed-leak, LFSR/BM, and MT19937 624-output clone cases; 手工/外部工具辅助 remains for MT19937 partial 8-bit clone matrices, digest-only MT2 output, streamgame key observations when the standalone `key` artifact is missing, and lfsr3 local artifact drift. |
| `scripts/solve_golf_hard.py` | misc | `docs/ctf-casebook.md` TJCTF golf-hard; `docs/ctf-playbook.md` Misc Method Card | 手工/外部工具辅助: local replay supplies five bounded recursive regex patterns to the provided service, shims the display-only `tabulate` dependency, and captures the service-returned flag. |
| `scripts/solve_attack_of_the_worm.py` | misc / ML | `docs/ctf-casebook.md` UMDCTF attack of the worm; `docs/ctf-playbook.md` Misc Method Card | 手工/外部工具辅助: local Docker replay applies at most 30 adversarial pixel changes to the worm image, feeds the payload to the provided ResNet18 service, and captures the service-returned flag. The helper also has `--score-only`, `--payload-file`, and parameterized `--search-unstable` modes with `--search-seeds`, `--search-output`, and `--payload-output` persistence so candidate payloads can be generated across seed sweeps, cached, rescored, and replayed with the exact single-image server preprocessing/model path. |
| `scripts/solve_unbreakable.py` | misc / python | `docs/ctf-casebook.md` HTB Unbreakable; `docs/ctf-playbook.md` Misc Method Card | 手工/外部工具辅助: source-only replay parses the Python blacklist, proves the `print(open(...).read())#` payload is allowed, and injects a local `flag.txt` fixture because the remote flag file was not shipped. |
| `scripts/solve_ee2026.py` | misc / hardware | `docs/ctf-casebook.md` NUS EE2026; `docs/ctf-playbook.md` Misc Method Card | 手工/外部工具辅助: local replay treats Vivado `.dcp` checkpoints as ZIP containers, parses `main.edf`, simulates the LUT netlist, and maps active-low seven-segment/anode outputs back to the assignment table. |
| `scripts/solve_i_see.py` | misc / hardware | `docs/ctf-casebook.md` DUCTF I See; `docs/ctf-playbook.md` Misc Method Card | 手工/外部工具辅助: schematic-backed hardware-source replay extracts the M24C02/I2C clues from the PDF and recovers the flag from the local EEPROM dump. |
| `scripts/solve_cecure_cerver.py` | web | `docs/ctf-casebook.md` NUS Cecure Cerver; `docs/ctf-playbook.md` Web Method Card | 手工/外部工具辅助: local replay compiles the C challenge source, brute forces one-character Basic Auth prefixes caused by `strncmp(..., strlen(input))`, and captures the returned flag from the HTTP response. |
| `scripts/solve_private_hidden_paths.py` | web | `docs/ctf-casebook.md` NUS Private Hidden Paths; `docs/ctf-playbook.md` Web Method Card | 手工/外部工具辅助: local Docker replay exploits PHP `pack()` format rewind operators, mints a pro token, joins `/pro` with `c/self/root/flag.txt`, and captures the returned flag over HTTP. |
| `scripts/solve_fetcher.py` | web | `docs/ctf-casebook.md` TJCTF fetcher; `docs/ctf-playbook.md` Web Method Card | 手工/外部工具辅助: local Docker replay starts the Bun/Express service and posts a `127.0.0.2` loopback-alias SSRF URL to reach the source-only `/flag` route. |
| `scripts/solve_co2.py` | web | `docs/ctf-casebook.md` DUCTF co2; `docs/ctf-playbook.md` Web Method Card | 手工/外部工具辅助: local Python replay registers a throwaway user, submits a nested `__class__.__init__.__globals__.flag` feedback payload, and captures `/get_flag`. |
| `scripts/solve_http_fanatics.py` | web | `docs/ctf-casebook.md` UMDCTF HTTP Fanatics; `docs/ctf-playbook.md` Web Method Card | 手工/外部工具辅助: local FastAPI replay sends the HTTP/1.1 bytes produced by the HTTP/3 proxy, smuggles `POST /admin/register`, and captures the dashboard flag. |
| `scripts/solve_lamenote.py` | web | `docs/ctf-casebook.md` IrisCTF LameNote; `docs/ctf-playbook.md` Web Method Card | 手工/外部工具辅助: source-pattern replay identifies the iframe-gated owner-scoped substring search oracle, demonstrates prefix recovery against a local synthetic admin note, and emits the manifest flag pattern when no concrete remote adminbot flag is shipped. |
| `scripts/solve_bof_school.py` | pwn | `docs/ctf-casebook.md` NUS Stack BOF School; `docs/ctf-playbook.md` Pwn Method Card | 手工/外部工具辅助: local Linux-container replay parses the `win` symbol, builds the challenge's escaped-byte ret2win payload, rejects placeholder training flags, and captures the service flag. |
| `scripts/solve_pac_shell.py` | pwn | `docs/ctf-casebook.md` DUCTF pac shell; `docs/ctf-playbook.md` Pwn Method Card | 手工/外部工具辅助: Docker-backed AArch64 replay derives PIE/libc from PAC-signed helper leaks and GOT reads, uses `environ` to locate the active stack, asks `help()` to sign a libc gadget, and reads `flag.txt` from the running challenge. |
| `scripts/solve_chisel.py` | pwn | `docs/ctf-casebook.md` UMDCTF chisel; `docs/ctf-playbook.md` Pwn Method Card | 手工/外部工具辅助: local amd64 Docker replay leaks heap and libc from freed chunks, poisons tcache with the safe-linking mask, overwrites `__malloc_hook` with `system`, and captures the running service flag. |
| `scripts/solve_maze_of_mist_static.py` | pwn | `docs/ctf-casebook.md` HTB Maze of Mist; `docs/ctf-playbook.md` Pwn Method Card | backlog_gap: static helper parses the shipped ret2vdso exploit constants and reports missing VM artifacts (`vmlinuz-linux`, `initramfs.cpio.gz`, `run.sh`, `target`); it deliberately does not accept README/writeup flags without a local VM replay. |
| `scripts/solve_epic_boss_fight.py` | pwn | `docs/ctf-casebook.md` NUS Epic Boss Fight / pwn01; `docs/ctf-playbook.md` Pwn Method Card | 手工/外部工具辅助: local Linux-container replay models signed 16-bit health wraparound, sends the minimal defend stream, rejects test flags, and emits the manifest-normalized flag prefix. |
| `scripts/solve_baby_heap.py` | pwn | `docs/ctf-casebook.md` TJCTF baby-heap; `docs/ctf-playbook.md` Pwn Method Card | 手工/外部工具辅助: local Linux-container replay uses a one-byte heap size overwrite and overlapping allocation to print the flag-bearing reader chunk. |
| `scripts/solve_insanity_check.py` | pwn | `docs/ctf-casebook.md` IrisCTF Insanity Check; `docs/ctf-playbook.md` Pwn Method Card | 手工/外部工具辅助: local Linux-container replay aligns the fixed suffix email's `.com\\0\\0\\0\\0` bytes over saved RIP to jump to the unusual `.flag` `win` symbol. |
| `scripts/solve_sign_in.py` | pwn | `docs/ctf-casebook.md` DUCTF sign-in; `docs/ctf-playbook.md` Pwn Method Card | 手工/外部工具辅助: local Linux-container replay uses freed user/list-entry chunk reuse and an uninitialized `next` pointer to sign in as uid 0 and read `flag.txt`. |

Maintenance rule:

- Add a row whenever a new `scripts/solve_*.py` helper lands.
- If the technique is automated, mark it as `底层 solver 已覆盖` and name the evidence field or transform.
- If the technique remains manual, mark it as `手工/外部工具辅助` and add an analysis hint when it is likely to recur through the Tools tab and `forgeflag hints`.
- Keep `README.md`, `CHANGELOG.md`, `docs/ctf-casebook.md`, and `docs/ctf-playbook.md` aligned when the status changes.
